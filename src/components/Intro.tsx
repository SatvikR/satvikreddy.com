import React from "react";
import { StyledLink } from "./StyledLink";
import { Text } from "@chakra-ui/react";

export const Intro: React.FC = () => {
  return (
    <Text my={4} fontSize="lg">
      I'm a senior at Aragon High School from the
      Bay Area.
    </Text>
  );
};
